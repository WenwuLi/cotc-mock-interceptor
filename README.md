# cotc-mock-interceptor - Chrome 扩展请求拦截插件

一个功能强大的 Chrome 浏览器扩展，用于拦截 XHR/Fetch 请求并返回配置的模拟数据。

## 功能特性

- ✅ 拦截浏览器发出的 XHR/Fetch 请求
- ✅ 支持多项目配置管理
- ✅ URL 模式匹配（支持通配符 `*`）
- ✅ JSON 格式的响应配置
- ✅ 项目启用/禁用控制
- ✅ 规则级别的启用/禁用控制
- ✅ 实时规则同步
- ✅ 规则快速复制功能
- ✅ 项目数据导入/导出（JSON格式）

## 使用场景

### 场景一：前后端并行开发

> 👨‍💻 **前端小明**：后端接口还没写好，但产品催着要看页面效果，我总不能干等着吧？
> 
> 🤔 **传统方案**：那你搭个 Express Mock 服务器呗，再配个代理，改改环境变量...
> 
> 👨‍💻 **前端小明**：就为了模拟几个接口，搞这么复杂？每次切环境还得重启服务？
> 
> 🔌 **本插件**：兄弟，打开我的配置页面，填个 URL 和响应数据，立马生效！想用真接口了？一键禁用规则就行，不用重启任何东西~

---

### 场景二：测试异常场景和边界情况

> 👩‍💻 **测试小红**：我需要测一下"用户不存在"、"权限不足"、"服务超时"这些异常情况...
> 
> 🤔 **传统方案**：让后端改代码？改数据库？还是用 Charles 抓包改响应？
> 
> 👩‍💻 **测试小红**：太麻烦了！而且改了可能影响别人开发...
> 
> 🔌 **本插件**：小意思！创建几个规则：
> - "用户不存在"：返回 `{ code: 404, message: "用户不存在" }`
> - "服务超时"：返回 `{ code: 500, message: "服务超时" }`
> - "无权限"：返回 `{ code: 403, message: "无权限访问" }`
> 
> 想测哪个场景就启用哪个规则，互不干扰，随心切换！

---

### 场景三：网络环境差或接口响应慢

> 👨‍💻 **前端小王**：公司网络今天又抽风了，一个接口请求要等 10 秒，我这一天啥也干不了...
> 
> 🤔 **传统方案**：忍着呗，或者找 IT 修网络？
> 
> 👨‍💻 **前端小王**：等 IT 来修，黄花菜都凉了！
> 
> 🔌 **本插件**：把那个慢接口拦截掉，返回本地 Mock 数据，响应速度？毫秒级！等网络好了或者要测真接口了，禁用规则就行~

---

### 场景四：多项目、多环境配置管理

> 👨‍💻 **全栈小李**：我同时维护 3 个项目，每个项目的 Mock 配置都不一样，环境变量配置文件一大堆，切来切去头都大了...
> 
> 🤔 **传统方案**：那就小心点呗，别配错了...
> 
> 👨‍💻 **全栈小李**：上次就是配错了，调了一下午的 bug 结果发现是环境搞混了！
> 
> 🔌 **本插件**：给每个项目建个独立配置空间呀！项目 A、项目 B、项目 C，各自的规则清清楚楚。要切项目？禁用一个、启用另一个，一秒搞定。还能导出配置分享给同事，团队配置统一不是梦！

---

### 场景五：接口频繁变更导致测试不稳定

> 👩‍💻 **前端小张**：后端又改接口了！我这边刚调好的功能又挂了...
> 
> 🤔 **传统方案**：等后端修复呗，或者你先改改代码适配一下？
> 
> 👩‍💻 **前端小张**：天天改，改不动了，我就想安安静静写个功能啊...
> 
> 🔌 **本插件**：理解你！把接口响应"锁定"住，配置稳定的 Mock 数据，任他后端改天改地，你这边岿然不动。等需要联调验证新接口了，禁用规则就能切回真实接口。还支持规则复制，相似场景的数据秒速搞定~

---

### 场景六：演示和演示环境准备

> 👔 **产品经理**：明天给大领导演示新功能，测试环境数据乱七八糟的，演示的时候接口别给我掉链子啊！
> 
> 👨‍💻 **前端小陈**：这我哪能保证啊，万一演示到一半接口报错...
> 
> 🔌 **本插件**：提前建个"演示专用"项目，把演示要用到的接口全部配好完美的响应数据。演示时启用这个项目，数据稳定、响应秒回，演示效果杠杠的！演示完了禁用项目，继续正常开发，互不影响~
> 
> 👔 **产品经理**：这个好！以后演示我心里有底了！

---

### 场景七：生产问题排查但拿不到生产数据

> 👨‍💻 **前端小周**：用户反馈线上有个奇怪的 bug，但我本地复现不了，数据对不上...
> 
> 🔒 **运维大哥**：生产数据？那可不行，里面有用户隐私信息，不能直接给你。
> 
> 👨‍💻 **前端小周**：那我怎么排查啊？总不能盲猜吧...
> 
> 🔒 **运维大哥**：我可以给你一份脱敏后的数据结构，具体值你自己模拟。
> 
> 👨‍💻 **前端小周**：好吧...但我要一个个手动改接口返回值，太麻烦了...
> 
> 🔌 **本插件**：这活我熟！把脱敏后的数据结构配成 Mock 规则，访问线上页面时自动拦截接口返回模拟数据。页面逻辑用的是线上代码，数据用的是你配的 Mock，完美复现问题场景！排查完了禁用规则，又能看到真实线上效果了~
> 
> 👨‍💻 **前端小周**：妙啊！不用改代码、不用部署，直接在浏览器里就能模拟生产数据结构来调试！

## 技术栈

- **前端框架**: Vue 3 + TypeScript
- **UI 组件库**: Ant Design Vue 4.x
- **构建工具**: Vite
- **Chrome API**: Manifest V3 + declarativeNetRequest

## 项目结构

```
cotc-mock-interceptor/
├── manifest.json              # Chrome 扩展配置文件
├── src/
│   ├── background/            # 后台脚本（Service Worker）
│   │   └── service-worker.ts  # 处理请求拦截逻辑
│   ├── options/               # Options 页面
│   │   ├── index.html         # Options 页面入口
│   │   ├── main.ts            # Options 页面入口脚本
│   │   └── App.vue            # Options 页面主组件
│   ├── components/            # Vue 组件
│   │   ├── ProjectList.vue    # 项目列表组件
│   │   ├── ProjectCard.vue    # 项目卡片组件
│   │   ├── InterceptionList.vue # 拦截规则列表组件
│   │   └── OverrideModal.vue  # 编辑拦截规则弹窗组件
│   ├── stores/                # 状态管理
│   │   └── useStorage.ts      # Chrome Storage 封装
│   ├── types/                 # TypeScript 类型定义
│   │   └── index.ts           # 项目、规则等类型定义
│   └── utils/                 # 工具函数
│       ├── urlMatcher.ts      # URL 匹配工具
│       └── storage.ts         # 存储操作工具
└── vite.config.ts             # Vite 配置
```

## 安装使用

### 从 Release 下载

如果您只是想使用本扩展，无需从源码构建，可以直接下载最新版本：

1. 访问 [GitHub Releases 页面](https://github.com/WenwuLi/cotc-mock-interceptor/releases)
2. 找到最新的 Release 版本（如 `v1.0.0`）
3. 在 "Assets" 部分下载 `cotc-mock-interceptor_*.zip` 文件

**安装步骤**：

1. 解压下载的 ZIP 文件到本地任意目录
2. 打开 Chrome 浏览器，访问 `chrome://extensions/`
3. 开启"开发者模式"（右上角开关）
4. 点击"加载已解压的扩展程序"
5. 选择刚才解压后的文件夹
6. 安装完成！点击扩展图标即可开始使用

> **提示**: 每次 Chrome 浏览器更新或重启后，扩展仍会保持启用状态，无需重新安装。

### 加载扩展到 Chrome

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"（右上角开关）
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `dist` 目录

## 使用说明

### 创建项目

1. 在 Options 页面点击"新建项目"按钮
2. 输入项目名称
![新建项目](https://i.mji.rip/2026/03/22/e71c65c7b3dee394dcc94bbec597bc56.png)
3. 创建成功后，项目会出现在项目列表中

### 配置拦截规则
1. 点击项目卡片进入拦截规则管理页面
2. 点击"创建XHR拦截"按钮
3. 填写以下信息：
   - **规则名称**: 为规则起一个容易识别的名称
   - **目标URL模式**: 支持通配符，如 `/api/user/info` 或 `*/api/*`
   - **响应内容**: JSON 格式的响应数据
   - **立即启用此规则**: 是否在创建后立即启用

### URL 模式匹配

支持以下匹配模式：

- 精确匹配: `/api/user/info`
- 通配符匹配: `*/api/*` (匹配所有包含 `/api/` 的请求)
- 路径匹配: `/api/*` (匹配所有以 `/api/` 开头的请求)
![配置拦截规则](https://i.mji.rip/2026/03/22/feadacbea9c437852bc6eb209640c687.png)
![效果](https://i.mji.rip/2026/03/22/20d284a26c4af774b7e95b83bc5d01d2.png)

### 启用/禁用

- **项目级别**: 在项目卡片上切换开关，控制整个项目的所有规则
- **规则级别**: 在规则列表中勾选/取消勾选，控制单个规则

### 复制规则

1. 在拦截规则列表中，找到想要复制的规则
2. 点击规则操作栏中的"复制"按钮
3. 系统会自动创建一个新规则，规则名称后缀为 "(副本)"
4. 可以编辑复制后的规则以满足新的需求
![复制规则](https://i.mji.rip/2026/03/22/5e069c67b82cbcc5c68a7abcc60c36d1.png)

### 导出项目数据

1. 在项目卡片的操作菜单中点击"导出项目"
2. 系统会自动下载一个 JSON 文件，文件名格式为 `项目名称_YYYYMMDD_HHmmss.json`
3. 该文件包含项目的所有配置信息和拦截规则
4. 可以用于备份或在其他设备上导入

### 导入项目数据

1. 在 Options 页面点击"导入项目"按钮
2. 选择之前导出的 JSON 文件
3. 系统会验证文件格式：
   - 如果项目名称已存在，会提示是否覆盖
   - 如果文件格式不正确，会显示错误提示
4. 导入成功后，项目及其所有规则会自动添加到当前配置中
![复制规则](https://i.mji.rip/2026/03/22/7a3d2606bdb0619f4456c51759ad324a.png)

## 注意事项

1. 扩展使用 Manifest V3，需要 Chrome 88+ 版本
2. 拦截规则需要同时满足以下条件才会生效：
   - 项目已启用
   - 规则已启用
3. 修改规则后会自动同步到 Chrome，无需手动刷新
4. 数据存储在 `chrome.storage.local`，仅本地存储

## 致谢

🙏 **致谢**

感谢以下资源和社区：

- Chrome Extensions API 官方文档
- Vue 3 和 Ant Design Vue 社区
- 所有贡献者和使用者

---

⭐ 觉得好用？给个 Star 支持一下！

[⭐ Star on GitHub](https://github.com/WenwuLi/cotc-mock-interceptor)

---

## 许可证

MIT
